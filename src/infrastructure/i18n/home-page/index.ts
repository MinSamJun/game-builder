import { enBlocks, enItems } from "./en";
import { jaBlocks, jaItems } from "./ja";
import { koBlocks, koItems } from "./ko";

export const en = {
  blocks: enBlocks,
  items: enItems,
};

export const ja = {
  blocks: jaBlocks,
  items: jaItems,
};

export const ko = {
  blocks: koBlocks,
  items: koItems,
};
